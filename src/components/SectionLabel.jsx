export default function SectionLabel({ label }) {
  return (
    <span className="inline-block text-xs font-mono font-medium tracking-[0.2em] text-saffron uppercase mb-4">
      {label}
    </span>
  )
}
